/**5. Conteo de elementos
Dado un arreglo de strings, retorna cuántos elementos tiene el arreglo. */
const lista = ["sebas", "carol", "tony"];
let contador = 0;
lista.forEach(() => contador++);
console.log(contador);